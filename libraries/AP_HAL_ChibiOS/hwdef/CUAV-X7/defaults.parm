# 温度控制参数配置
# 由于BMI088 IMU没有出厂温度校准,需要进行温度控制
# 而ADIS16470已经过出厂温度校准,但仍需要温度控制以提高精度
# 使用较低的P/I值可以防止BMI088温度振荡
BRD_HEAT_TARG 45    # 设置目标温度为45度
BRD_HEAT_P 50       # 温度控制比例系数
BRD_HEAT_I 0.07     # 温度控制积分系数

# CAN总线电源监测配置
CAN_P1_DRIVER 1     # 启用CAN1端口的电源监测驱动
BATT_MONITOR 8      # 设置电池监测模式为8(CAN总线电源监测)

# ADC电源模块参数配置
BATT_VOLT_PIN 16    # 电池电压采样引脚为ADC通道16
BATT_CURR_PIN 17    # 电池电流采样引脚为ADC通道17
BATT_VOLT_MULT 18.000   # 电压测量倍率为18.0(根据分压电路校准)
BATT_AMP_PERVLT 24.000  # 电流测量系数为24.0安培/伏特(根据采样电路校准)
