# 这是mRo Control Zero OEM H7飞控板的默认参数配置文件
# 该文件定义了飞控板启动时的基本参数设置

# 设置CAN总线1的驱动类型为1(UAVCAN)
# CAN总线用于飞控与其他设备(如GPS、电调等)的通信
CAN_P1_DRIVER 1

# 设置CAN总线2的驱动类型为2(KDECAN) 
# 双CAN总线设计可以分别用于不同设备的通信,提高系统稳定性
CAN_P2_DRIVER 2
